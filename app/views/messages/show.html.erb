<div class="card my-3 ">
  <h5 class="card-header">
    <% if @message.outbox.user.is_doctor? %>Dr.<% end %>
    <%= @message.outbox.user.full_name %>
  </h5>
  <div class="card-body">
    <p class="card-text"><%= @message.body %></p>
    <% if @message.inbox.user.is_patient? %>
    <%= link_to "Reply", new_message_path, class: "btn btn-primary" %>
    <%= link_to "I've lost my script, charge me €10", issuing_scripts_path, method: :post, class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
